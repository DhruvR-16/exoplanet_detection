# Project Agent Rules

- **Commit After Changes**: Always make a git commit immediately after performing file changes, code edits, or cleanups.
- **Commit Directly to Main**: All git commits must be made directly to the `main` branch. Do NOT create or switch to separate feature branches.
